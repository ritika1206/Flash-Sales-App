<%= form_with model: @user, url: user_forgot_password_mail_sent_users_url, method: :get do |form| %>
  <%= form.label :email, 'Enter your registered Email' %>
  <%= form.email_field :email%>

  <button type="submit">Submit</button>
<% end %>
